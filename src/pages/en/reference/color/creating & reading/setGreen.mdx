---
layout: '@layouts/reference/SingleReferenceLayout.astro'
title: setGreen
module: Color
submodule: Creating & Reading
file: /Users/stalgia/Desktop/Work/p5-scratch-pad/temp/p5.js/src/color/p5.Color.js
descriptionText: |-
  Sets the green component of a color. The range depends on the
  <a href="#/colorMode">colorMode()</a>. In the default RGB mode it's
  between 0 and 255.
params:
  - name: green
    description: the new green value.
    type: Number
kind: function
name: setGreen
namespace: setGreen
examples:
  - |-
    <div>
    <code>
    let backgroundColor;

    function setup() {
      backgroundColor = color(100, 50, 150);
    }

    function draw() {
      backgroundColor.setGreen(128 + 128 * sin(millis() / 1000));
      background(backgroundColor);
      describe('A canvas with a gradually changing background color.');
    }
    </code>
    </div>
    *
---


# setGreen
