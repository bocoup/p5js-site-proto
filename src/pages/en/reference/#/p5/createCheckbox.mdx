---
layout: '@layouts/reference/SingleReferenceLayout.astro'
title: createCheckbox
module: DOM
submodule: DOM
file: in/p5.js/src/dom/dom.js
description: >
  <p>Creates a checkbox <code>&lt;input&gt;&lt;/input&gt;</code> element.
  Checkboxes extend

  the <a href="#/p5.Element">p5.Element</a> class with a <code>checked()</code>
  method.

  Calling <code>myBox.checked()</code> returns <code>true</code> if it the box
  is checked and

  <code>false</code> otherwise.</p>

  <p>The first parameter, <code>label</code>, is optional. It's a string that
  sets the label

  to display next to the checkbox.</p>

  <p>The second parameter, <code>value</code>, is also optional. It's a boolean
  that sets the

  checkbox's value.</p>
params:
  - name: label
    description: |
      <p>label displayed after the checkbox.</p>
    type: String
    optional: true
  - name: value
    description: >
      <p>value of the checkbox. Checked is <code>true</code> and unchecked is
      <code>false</code>.</p>
    type: Boolean
    optional: true
line: 914
itemtype: method
name: createCheckbox
class: p5
examples: []
---


# createCheckbox
