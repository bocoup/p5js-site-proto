---
layout: '@layouts/reference/SingleReferenceLayout.astro'
title: sqrt
module: Math
submodule: Calculation
file: in/p5.js/src/math/calculation.js
description: >
  <p>Calculates the square root of a number. A number's square root can be

  multiplied by itself to produce the original number.</p>

  <p>For example, <code>sqrt(9)</code> returns 3 because 3 × 3 = 9.
  <code>sqrt()</code> always

  returns a positive value. <code>sqrt()</code> doesn't work with negative
  arguments

  such as <code>sqrt(-9)</code>.</p>
params:
  - name: 'n'
    description: |
      <p>non-negative number to square root.</p>
    type: Number
line: 700
itemtype: method
name: sqrt
class: p5
examples: []
---


# sqrt
