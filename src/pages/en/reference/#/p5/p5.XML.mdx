---
layout: '@layouts/reference/ClassReferenceLayout.astro'
title: p5.XML
module: IO
submodule: Input
file: src/io/p5.XML.js
description: >
  <p>XML is a representation of an XML object, able to parse XML code. Use

  <a href="#/p5/loadXML">loadXML()</a> to load external XML files and create XML
  objects.</p>
isConstructor: true
line: 9
getParent:
  description: |
    <p>Gets a copy of the element's parent. Returns the parent as another
    <a href="#/p5.XML">p5.XML</a> object.</p>
  path: ./src/pages/en/reference/#/p5.XML/
getName:
  description: |
    <p>Gets the element's full name, which is returned as a String.</p>
  path: ./src/pages/en/reference/#/p5.XML/
setName:
  description: |
    <p>Sets the element's name, which is specified as a String.</p>
  path: ./src/pages/en/reference/#/p5.XML/
hasChildren:
  description: >
    <p>Checks whether or not the element has any children, and returns the
    result

    as a boolean.</p>
  path: ./src/pages/en/reference/#/p5.XML/
listChildren:
  description: >
    <p>Get the names of all of the element's children, and returns the names as
    an

    array of Strings. This is the same as looping through and calling <a
    href="#/p5.XML/getName">getName()</a>

    on each child element individually.</p>
  path: ./src/pages/en/reference/#/p5.XML/
getChildren:
  description: >
    <p>Returns all of the element's children as an array of <a
    href="#/p5.XML">p5.XML</a> objects. When

    the name parameter is specified, then it will return all children that match

    that name.</p>
  path: ./src/pages/en/reference/#/p5.XML/
getChild:
  description: >
    <p>Returns the first of the element's children that matches the name
    parameter

    or the child of the given index.It returns undefined if no matching

    child is found.</p>
  path: ./src/pages/en/reference/#/p5.XML/
addChild:
  description: >
    <p>Appends a new child to the element. The child can be specified with

    either a String, which will be used as the new tag's name, or as a

    reference to an existing <a href="#/p5.XML">p5.XML</a> object.

    A reference to the newly created child is returned as an <a
    href="#/p5.XML">p5.XML</a> object.</p>
  path: ./src/pages/en/reference/#/p5.XML/
removeChild:
  description: |
    <p>Removes the element specified by name or index.</p>
  path: ./src/pages/en/reference/#/p5.XML/
getAttributeCount:
  description: >
    <p>Counts the specified element's number of attributes, returned as an
    Number.</p>
  path: ./src/pages/en/reference/#/p5.XML/
listAttributes:
  description: |
    <p>Gets all of the specified element's attributes, and returns them as an
    array of Strings.</p>
  path: ./src/pages/en/reference/#/p5.XML/
hasAttribute:
  description: |
    <p>Checks whether or not an element has the specified attribute.</p>
  path: ./src/pages/en/reference/#/p5.XML/
getNum:
  description: >
    <p>Returns an attribute value of the element as an Number. If the
    defaultValue

    parameter is specified and the attribute doesn't exist, then defaultValue

    is returned. If no defaultValue is specified and the attribute doesn't

    exist, the value 0 is returned.</p>
  path: ./src/pages/en/reference/#/p5.XML/
getString:
  description: >
    <p>Returns an attribute value of the element as an String. If the
    defaultValue

    parameter is specified and the attribute doesn't exist, then defaultValue

    is returned. If no defaultValue is specified and the attribute doesn't

    exist, null is returned.</p>
  path: ./src/pages/en/reference/#/p5.XML/
setAttribute:
  description: |
    <p>Sets the content of an element's attribute. The first parameter specifies
    the attribute name, while the second specifies the new content.</p>
  path: ./src/pages/en/reference/#/p5.XML/
getContent:
  description: |
    <p>Returns the content of an element. If there is no such content,
    defaultValue is returned if specified, otherwise null is returned.</p>
  path: ./src/pages/en/reference/#/p5.XML/
setContent:
  description: |
    <p>Sets the element's content.</p>
  path: ./src/pages/en/reference/#/p5.XML/
serialize:
  description: >
    <p>Serializes the element into a string. This function is useful for
    preparing

    the content to be sent over a http request or saved to file.</p>
  path: ./src/pages/en/reference/#/p5.XML/
---


# p5.XML
