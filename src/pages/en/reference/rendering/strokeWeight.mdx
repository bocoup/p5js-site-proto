---
layout: '@layouts/reference/SingleReferenceLayout.astro'
title: strokeWeight
module: Rendering
file: >-
  /Users/stalgia/Desktop/Work/p5-scratch-pad/temp/p5.js/src/webgl/p5.RendererGL.js
descriptionText: Change weight of stroke
params:
  - name: stroke
    description: weight to be used for drawing
    type: Number
kind: function
name: strokeWeight
namespace: strokeWeight
examples:
  - |-
    <div>
    <code>
    function setup() {
      createCanvas(200, 400, WEBGL);
      setAttributes('antialias', true);
    }

    function draw() {
      background(0);
      noStroke();
      translate(0, -100, 0);
      stroke(240, 150, 150);
      fill(100, 100, 240);
      push();
      strokeWeight(8);
      rotateX(frameCount * 0.01);
      rotateY(frameCount * 0.01);
      sphere(75);
      pop();
      push();
      translate(0, 200, 0);
      strokeWeight(1);
      rotateX(frameCount * 0.01);
      rotateY(frameCount * 0.01);
      sphere(75);
      pop();
    }
    </code>
    </div>
---


# strokeWeight
