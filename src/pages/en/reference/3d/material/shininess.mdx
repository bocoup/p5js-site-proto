---
layout: '@layouts/reference/SingleReferenceLayout.astro'
title: shininess
module: 3D
submodule: Material
file: /Users/stalgia/Desktop/Work/p5-scratch-pad/temp/p5.js/src/webgl/material.js
descriptionText: >-
  Sets the amount of gloss ("shininess") of a

  <a href="#/p5/specularMaterial">specularMaterial()</a>.The default and minimum
  value is 1.
params:
  - name: shine
    description: degree of shininess
    type: Number
kind: function
name: shininess
namespace: shininess
examples:
  - |-
    <div>
    <code>
    function setup() {
      createCanvas(100, 100, WEBGL);
      describe('two spheres, one more shiny than the other');
    }
    function draw() {
      background(0);
      noStroke();
      let locX = mouseX - width / 2;
      let locY = mouseY - height / 2;
      ambientLight(60, 60, 60);
      pointLight(255, 255, 255, locX, locY, 50);
      specularMaterial(250);
      translate(-25, 0, 0);
      shininess(1);
      sphere(20);
      translate(50, 0, 0);
      shininess(20);
      sphere(20);
    }
    </code>
    </div>
---


# shininess
