---
layout: '@layouts/reference/SingleReferenceLayout.astro'
title: lookAt
module: 3D
submodule: Camera
file: /Users/stalgia/Desktop/Work/p5-scratch-pad/temp/p5.js/src/webgl/p5.Camera.js
descriptionText: Reorients the camera to look at a position in world space.
params:
  - name: x
    description: x position of a point in world space
    type: Number
  - name: 'y'
    description: y position of a point in world space
    type: Number
  - name: z
    description: z position of a point in world space
    type: Number
kind: function
name: lookAt
namespace: lookAt
examples:
  - |-
    <div>
    <code>
    let cam;

    function setup() {
      createCanvas(100, 100, WEBGL);
      normalMaterial();
      cam = createCamera();
    }

    function draw() {
      background(200);

      // look at a new random point every 60 frames
      if (frameCount % 60 === 0) {
        cam.lookAt(random(-100, 100), random(-50, 50), 0);
      }

      rotateX(frameCount * 0.01);
      translate(-100, 0, 0);
      box(20);
      translate(35, 0, 0);
      box(20);
      translate(35, 0, 0);
      box(20);
      translate(35, 0, 0);
      box(20);
      translate(35, 0, 0);
      box(20);
      translate(35, 0, 0);
      box(20);
      translate(35, 0, 0);
      box(20);
    }
    </code>
    </div>
---


# lookAt
