---
layout: '@layouts/reference/SingleReferenceLayout.astro'
title: move
module: 3D
submodule: Camera
file: /Users/stalgia/Desktop/Work/p5-scratch-pad/temp/p5.js/src/webgl/p5.Camera.js
descriptionText: Move camera along its local axes while maintaining current camera orientation.
params:
  - name: x
    description: amount to move along camera's left-right axis
    type: Number
  - name: 'y'
    description: amount to move along camera's up-down axis
    type: Number
  - name: z
    description: amount to move along camera's forward-backward axis
    type: Number
kind: function
name: move
namespace: move
examples:
  - |-
    <div>
    <code>
    // see the camera move along its own axes while maintaining its orientation
    let cam;
    let delta = 0.5;

    function setup() {
      createCanvas(100, 100, WEBGL);
      normalMaterial();
      cam = createCamera();
    }

    function draw() {
      background(200);

      // move the camera along its local axes
      cam.move(delta, delta, 0);

      // every 100 frames, switch direction
      if (frameCount % 150 === 0) {
        delta *= -1;
      }

      translate(-10, -10, 0);
      box(50, 8, 50);
      translate(15, 15, 0);
      box(50, 8, 50);
      translate(15, 15, 0);
      box(50, 8, 50);
      translate(15, 15, 0);
      box(50, 8, 50);
      translate(15, 15, 0);
      box(50, 8, 50);
      translate(15, 15, 0);
      box(50, 8, 50);
    }
    </code>
    </div>
---


# move
