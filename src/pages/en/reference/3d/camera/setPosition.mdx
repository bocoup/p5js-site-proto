---
layout: '@layouts/reference/SingleReferenceLayout.astro'
title: setPosition
module: 3D
submodule: Camera
file: /Users/stalgia/Desktop/Work/p5-scratch-pad/temp/p5.js/src/webgl/p5.Camera.js
descriptionText: |-
  Set camera position in world-space while maintaining current camera
  orientation.
params:
  - name: x
    description: x position of a point in world space
    type: Number
  - name: 'y'
    description: y position of a point in world space
    type: Number
  - name: z
    description: z position of a point in world space
    type: Number
kind: function
name: setPosition
namespace: setPosition
examples:
  - |-
    <div>
    <code>
    // press '1' '2' or '3' keys to set camera position

    let cam;

    function setup() {
      createCanvas(100, 100, WEBGL);
      normalMaterial();
      cam = createCamera();
    }

    function draw() {
      background(200);

      // '1' key
      if (keyIsDown(49)) {
        cam.setPosition(30, 0, 80);
      }
      // '2' key
      if (keyIsDown(50)) {
        cam.setPosition(0, 0, 80);
      }
      // '3' key
      if (keyIsDown(51)) {
        cam.setPosition(-30, 0, 80);
      }

      box(20);
    }
    </code>
    </div>
---


# setPosition
