---
layout: '@layouts/reference/SingleReferenceLayout.astro'
title: onended
module: DOM
submodule: DOM
file: src/dom/dom.js
description: >
  <p>Calls a function when the audio/video reaches the end of its playback

  The function won't be called if the media is looping.</p>

  <p>The <code>p5.MediaElement</code> is passed as an argument to the callback
  function.</p>
line: 4370
params:
  - name: callback
    description: |
      <p>function to call when playback ends.
                                  The <code>p5.MediaElement</code> is passed as
                                  the argument.</p>
    type: Function
itemtype: method
class: p5.MediaElement
chainable: true
---


# onended
