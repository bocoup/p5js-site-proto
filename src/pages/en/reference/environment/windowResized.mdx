---
layout: '@layouts/reference/SingleReferenceLayout.astro'
title: windowResized
module: Environment
submodule: Environment
file: in/p5.js/src/core/environment.js
descriptionText: >
  <p>The code in <code>windowResized()</code> is called once each time the
  browser window

  is resized. It's a good place to resize the canvas or make other

  adjustments to accommodate the new window size.</p>

  <p>The <code>event</code> parameter is optional. If added to the function
  definition, it

  can be used for debugging or other purposes.</p>
params:
  - name: event
    description: |
      <p>optional resize Event.</p>
    type: UIEvent
    optional: true
line: 661
description: >
  <p>The code in <code>windowResized()</code> is called once each time the
  browser window

  is resized. It's a good place to resize the canvas or make other

  adjustments to accommodate the new window size.</p>

  <p>The <code>event</code> parameter is optional. If added to the function
  definition, it

  can be used for debugging or other purposes.</p>
itemtype: method
name: windowResized
alt: This example does not render anything.
class: p5
examples: []
---


# windowResized
