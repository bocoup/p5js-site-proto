---
layout: '@layouts/reference/SingleReferenceLayout.astro'
title: splitTokens
module: Data
submodule: String Functions
file: >-
  /Users/stalgia/Desktop/Work/p5-scratch-pad/temp/p5.js/src/utilities/string_functions.js
descriptionText: >-
  The <a href="#/p5/splitTokens">splitTokens()</a> function splits a String at
  one or many character

  delimiters or "tokens." The delim parameter specifies the character or

  characters to be used as a boundary.If no delim characters are specified, any
  whitespace character is used to

  split. Whitespace characters include tab (\t), line feed (\n), carriage

  return (\r), form feed (\f), and space.
params:
  - name: value
    description: the String to be split
    type: String
  - name: delim
    description: |-
      list of individual Strings that will be used as
      separators
    type: String
kind: function
name: splitTokens
namespace: splitTokens
examples:
  - |-
    <div class = "norender">
    <code>
    function setup() {
      let myStr = 'Mango, Banana, Lime';
      let myStrArr = splitTokens(myStr, ',');

      print(myStrArr); // prints : ["Mango"," Banana"," Lime"]
    }
    </code>
    </div>
---


# splitTokens
