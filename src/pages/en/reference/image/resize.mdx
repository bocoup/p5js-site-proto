---
layout: '@layouts/reference/SingleReferenceLayout.astro'
title: resize
module: Image
submodule: Image
file: in/p5.js/src/image/p5.Image.js
descriptionText: >
  <p>Resizes the <a href="#/p5.Image">p5.Image</a> object to a given
  <code>width</code>

  and <code>height</code>. The image's original aspect ratio can be kept by
  passing 0

  for either <code>width</code> or <code>height</code>. For example, calling
  <code>img.resize(50, 0)</code>

  on an image that was 500 × 300 pixels will resize it to

  50 × 30 pixels.</p>
params:
  - name: width
    description: |
      <p>resized image width.</p>
    type: Number
  - name: height
    description: |
      <p>resized image height.</p>
    type: Number
line: 626
description: >
  <p>Resizes the <a href="#/p5.Image">p5.Image</a> object to a given
  <code>width</code>

  and <code>height</code>. The image's original aspect ratio can be kept by
  passing 0

  for either <code>width</code> or <code>height</code>. For example, calling
  <code>img.resize(50, 0)</code>

  on an image that was 500 × 300 pixels will resize it to

  50 × 30 pixels.</p>
itemtype: method
name: resize
class: p5.Image
examples: []
---


# resize
