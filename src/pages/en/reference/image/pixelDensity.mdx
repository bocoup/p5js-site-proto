---
layout: '@layouts/reference/SingleReferenceLayout.astro'
title: pixelDensity
module: Image
submodule: Image
file: in/p5.js/src/image/p5.Image.js
descriptionText: |
  <p>Gets or sets the pixel density for high pixel density displays. By default,
  the density will be set to 1.</p>
  <p>Call this method with no arguments to get the default density, or pass
  in a number to set the density. If a non-positive number is provided,
  it defaults to 1.</p>
params:
  - name: density
    description: |
      <p>A scaling factor for the number of pixels per
      side</p>
    type: Number
    optional: true
line: 227
description: |
  <p>Gets or sets the pixel density for high pixel density displays. By default,
  the density will be set to 1.</p>
  <p>Call this method with no arguments to get the default density, or pass
  in a number to set the density. If a non-positive number is provided,
  it defaults to 1.</p>
itemtype: method
name: pixelDensity
class: p5.Image
examples: []
---


# pixelDensity
