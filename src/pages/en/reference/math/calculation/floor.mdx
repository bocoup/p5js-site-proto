---
layout: '@layouts/reference/SingleReferenceLayout.astro'
title: floor
module: Math
submodule: Calculation
file: /Users/stalgia/Desktop/Work/p5-scratch-pad/temp/p5.js/src/math/calculation.js
descriptionText: |-
  Calculates the closest integer value that is less than or equal to the
  value of the `n` parameter.
params:
  - name: 'n'
    description: number to round down.
    type: Number
kind: function
name: floor
namespace: floor
examples:
  - >-
    <div>

    <code>

    // Set the range for RGB values from 0 to 1.

    colorMode(RGB, 1);

    noStroke();


    let r = 0.8;

    fill(r, 0, 0);

    rect(0, 0, width / 2, height);


    // Round r down to 0.

    r = floor(r);

    fill(r, 0, 0);

    rect(width / 2, 0, width / 2, height);


    describe('Two rectangles. The one on the left is bright red and the one on
    the right is black.');

    </code>

    </div>
---


# floor
