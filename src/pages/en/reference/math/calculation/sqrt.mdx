---
layout: '@layouts/reference/SingleReferenceLayout.astro'
title: sqrt
module: Math
submodule: Calculation
file: /Users/stalgia/Desktop/Work/p5-scratch-pad/temp/p5.js/src/math/calculation.js
descriptionText: >-
  Calculates the square root of a number. A number's square root can be

  multiplied by itself to produce the original number.For example, `sqrt(9)`
  returns 3 because 3 × 3 = 9. `sqrt()` always

  returns a positive value. `sqrt()` doesn't work with negative arguments

  such as `sqrt(-9)`.
params:
  - name: 'n'
    description: non-negative number to square root.
    type: Number
kind: function
name: sqrt
namespace: sqrt
examples:
  - |-
    <div>
    <code>
    function draw() {
      // Invert the y-axis.
      scale(1, -1);
      translate(0, -height);

      let x = frameCount;
      let y = 5 * sqrt(x);
      point(x, y);

      describe('A series of black dots that get higher slowly from left to right.');
    }
    </code>
    </div>
---


# sqrt
