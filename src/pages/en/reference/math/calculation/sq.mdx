---
layout: '@layouts/reference/SingleReferenceLayout.astro'
title: sq
module: Math
submodule: Calculation
file: /Users/stalgia/Desktop/Work/p5-scratch-pad/temp/p5.js/src/math/calculation.js
descriptionText: >-
  Squares a number, which means multiplying the number by itself. The value

  returned is always a positive number.For example, `sq(3)` evaluates 3 × 3 
  which is 9. `sq(-3)` evaluates

  -3 × -3 which is also 9.  Multiplying two negative numbers produces

  a positive number.
params:
  - name: 'n'
    description: number to square.
    type: Number
kind: function
name: sq
namespace: sq
examples:
  - |-
    <div>
    <code>
    function draw() {
      // Invert the y-axis.
      scale(1, -1);
      translate(0, -height);

      let x = frameCount;
      let y = 0.01 * sq(x);
      point(x, y);

      describe('A series of black dots that get higher quickly from left to right.');
    }
    </code>
    </div>
---


# sq
