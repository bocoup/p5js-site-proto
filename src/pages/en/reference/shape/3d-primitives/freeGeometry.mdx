---
layout: '@layouts/reference/SingleReferenceLayout.astro'
title: freeGeometry
module: Shape
submodule: 3D Primitives
file: in/p5.js/src/webgl/3d_primitives.js
descriptionText: |
  <p>Clears the resources of a model to free up browser memory. A model whose
  resources have been cleared can still be drawn, but the first time it is
  drawn again, it might take longer.</p>
  <p>This method works on models generated with
  <a href="#/p5/buildGeometry">buildGeometry()</a> as well as those loaded
  from <a href="#/p5/loadModel">loadModel()</a>.</p>
params:
  - name: geometry
    description: |
      <p>The geometry whose resources should be freed</p>
    type: p5.Geometry
line: 167
description: |
  <p>Clears the resources of a model to free up browser memory. A model whose
  resources have been cleared can still be drawn, but the first time it is
  drawn again, it might take longer.</p>
  <p>This method works on models generated with
  <a href="#/p5/buildGeometry">buildGeometry()</a> as well as those loaded
  from <a href="#/p5/loadModel">loadModel()</a>.</p>
itemtype: method
name: freeGeometry
class: p5
examples: []
---


# freeGeometry
