---
layout: '@layouts/reference/SingleReferenceLayout.astro'
title: hideControls
module: DOM
file: /Users/stalgia/Desktop/Work/p5-scratch-pad/temp/p5.js/src/dom/dom.js
descriptionText: Hide the default mediaElement controls.
params: []
kind: function
name: hideControls
namespace: hideControls
examples:
  - |-
    <div><code>
    let ele;
    function setup() {
      //p5.MediaElement objects are usually created
      //by calling the createAudio(), createVideo(),
      //and createCapture() functions.
      //In this example we create
      //a new p5.MediaElement via createAudio()
      ele = createAudio('assets/lucky_dragons.mp3');
      ele.showControls();
      background(200);
      textAlign(CENTER);
      text('Click to hide Controls!', 10, 25, 70, 80);
    }
    function mousePressed() {
      ele.hideControls();
      background(200);
      text('Controls hidden', width / 2, height / 2);
    }
    </code></div>
---


# hideControls
