---
layout: '@layouts/reference/SingleReferenceLayout.astro'
title: touchMoved
module: DOM
submodule: DOM
file: in/p5.js/src/core/p5.Element.js
descriptionText: |
  <p>Calls a function when the user touches the element and moves their
  finger. Calling <code>myElement.touchMoved(false)</code> disables the
  function.</p>
  <p>Note: Touch functions only work on mobile devices.</p>
params:
  - name: fxn
    description: |
      <p>function to call when the touch
                                     moves over the element.
                                     <code>false</code> disables the function.</p>
    type: Function|Boolean
line: 956
description: |
  <p>Calls a function when the user touches the element and moves their
  finger. Calling <code>myElement.touchMoved(false)</code> disables the
  function.</p>
  <p>Note: Touch functions only work on mobile devices.</p>
itemtype: method
name: touchMoved
chainable: 1
class: p5.Element
examples: []
---


# touchMoved
