---
layout: '@layouts/reference/SingleReferenceLayout.astro'
title: createDiv
module: DOM
submodule: DOM
file: in/p5.js/src/dom/dom.js
descriptionText: >
  <p>Creates a <code>&lt;div&gt;&lt;/div&gt;</code> element. It's commonly used
  as a

  container for other elements.</p>

  <p>The parameter <code>html</code> is optional. It accepts a string that sets
  the

  inner HTML of the new <code>&lt;div&gt;&lt;/div&gt;</code>.</p>
params:
  - name: html
    description: >
      <p>inner HTML for the new <code>&lt;div&gt;&lt;/div&gt;</code>
      element.</p>
    type: String
    optional: true
line: 444
description: >
  <p>Creates a <code>&lt;div&gt;&lt;/div&gt;</code> element. It's commonly used
  as a

  container for other elements.</p>

  <p>The parameter <code>html</code> is optional. It accepts a string that sets
  the

  inner HTML of the new <code>&lt;div&gt;&lt;/div&gt;</code>.</p>
itemtype: method
name: createDiv
class: p5
examples: []
---


# createDiv
