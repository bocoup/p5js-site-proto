---
layout: '@layouts/reference/SingleReferenceLayout.astro'
title: id
module: DOM
file: src/core/p5.Element.js
descriptionText: >-
  Sets this element's ID using a given string.Calling `myElement.id()` without
  an argument returns its ID as a string.
params:
  - name: id
    description: ID of the element.
    type: String
kind: function
name: id
namespace: id
examples:
  - |-
    <div>
    <code>
    function setup() {
      // Create a canvas element and
      // assign it to cnv.
      let cnv = createCanvas(100, 100);

      background(200);

      // Set the canvas' ID
      // to "mycanvas".
      cnv.id('mycanvas');

      // Get the canvas' ID.
      let id = cnv.id();
      text(id, 24, 54);

      describe('The text "mycanvas" written in black on a gray background.');
    }
    </code>
    </div>
---


# id
