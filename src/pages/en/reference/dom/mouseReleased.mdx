---
layout: '@layouts/reference/SingleReferenceLayout.astro'
title: mouseReleased
module: DOM
submodule: DOM
file: in/p5.js/src/core/p5.Element.js
descriptionText: |
  <p>Calls a function when the mouse is released over the element. Calling
  <code>myElement.mouseReleased(false)</code> disables the function.</p>
  <p>Note: Some mobile browsers may also trigger this event when the element
  receives a quick tap.</p>
params:
  - name: fxn
    description: |
      <p>function to call when the mouse is
                                     pressed over the element.
                                     <code>false</code> disables the function.</p>
    type: Function|Boolean
line: 573
description: |
  <p>Calls a function when the mouse is released over the element. Calling
  <code>myElement.mouseReleased(false)</code> disables the function.</p>
  <p>Note: Some mobile browsers may also trigger this event when the element
  receives a quick tap.</p>
itemtype: method
name: mouseReleased
chainable: 1
class: p5.Element
examples: []
---


# mouseReleased
