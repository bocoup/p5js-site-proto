---
layout: '@layouts/reference/SingleReferenceLayout.astro'
title: getRow
module: IO
submodule: Table
file: /Users/stalgia/Desktop/Work/p5-scratch-pad/temp/p5.js/src/io/p5.Table.js
descriptionText: >-
  Returns a reference to the specified <a href="#/p5.TableRow">p5.TableRow</a>.
  The reference

  can then be used to get and set values of the selected row.
params:
  - name: rowID
    description: ID number of the row to get
    type: Integer
kind: function
name: getRow
namespace: getRow
examples:
  - |-
    <div class="norender">
    <code>
    // Given the CSV file "mammals.csv"
    // in the project's "assets" folder:
    //
    // id,species,name
    // 0,Capra hircus,Goat
    // 1,Panthera pardus,Leopard
    // 2,Equus zebra,Zebra

    let table;

    function preload() {
      //my table is comma separated value "csv"
      //and has a header specifying the columns labels
      table = loadTable('assets/mammals.csv', 'csv', 'header');
    }

    function setup() {
      let row = table.getRow(1);
      //print it column by column
      //note: a row is an object, not an array
      for (let c = 0; c < table.getColumnCount(); c++) {
        print(row.getString(c));
      }

      describe('no image displayed');
    }
    </code>
    </div>
---


# getRow
