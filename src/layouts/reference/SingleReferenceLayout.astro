---
const {
  frontmatter: { title, module, submodule, examples },
} = Astro.props;
import CodeEmbed from "@components/reference/CodeEmbed/";
---

<html>
  <h1>{title}</h1>
  <h2>Module: {module}</h2>
  <p>Submodule: {submodule}</p>
  <br />
  <!-- This is the actual MDX content: -->
  <slot />
  <!-- This is a react component -->
  <CodeEmbed code={examples} />


</html>
